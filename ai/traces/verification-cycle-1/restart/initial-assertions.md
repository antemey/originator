# Phase C, cycle 1: independently derived initial assertions

Recorded on 2026-09-18 before engine execution or test design from implementation. Launch HEAD: `fa4acec5729b003ceeb3bf4a7dd64b3735bf3134`; `git status --porcelain=v1 --untracked-files=all` was empty; `AGENTS.override.md` marker was `verify`. No implementation, existing test source, previous verification trace, operator handoff, or model was read. The optional historical model will be skipped.

Reading order: active common/verify instructions; `docs/scope.md`; `ai/constitution.md`; `src/engine/contract.ts`; `target/context.md`; seed schema and three prepared seeds; four discovery fixtures and their associated sanitized captures. Historical status statements in these documents are not acceptance evidence. Package/tool configuration was read solely to identify execution entrypoints.

## Evidence and initial assertions

1. **Captured merchant decisions and projections.** Replay every action of `target-transitions` and `target-mixed-replay`, checking ordered acceptance, captured refusal codes/messages, all four money fields per line, coupon fields and four cart totals. Match identities, including variant, regardless of display order, without collapsing duplicates. Only the fixture's observed checkpoints are merchant expectations. Source: `ai/traces/discovery/phase-b-target-sequence.json` and `phase-b-target-replay.json`; SHA-256 matches each fixture's `capture_hash`.
2. **Captured recalculation.** Merchant sequence labels `remove-decouverte10`, `reapply-decouverte10`, `accessory-quantity-2`, `remove-accessory` establish removal/restoration of discount and accessory quantity recalculation. Accessory quantity 2 has 250/50 cents subtotal/tax and unchanged line total/tax. Coffee remains quantity 1 with 946/52 subtotal/tax and 851/47 discounted total/tax. These are observations, not a general merchant pricing algorithm.
3. **Captured refusal plus separate contract property.** Unknown coupon action 5 is refused and its following GET supplies an unchanged-cart observation. Coffee action 3 requests quantity 3 and has captured `readonly_quantity` and its exact message, but no immediate captured cart. Its non-mutation is asserted solely from scope/constitution, not invented merchant evidence. Requested and returned quantities must remain distinct.
4. **Captured lab projections.** Replay `lab-mirror-mixed` and `lab-two-percent-nonsequential` against their explicit lab seeds. Sources: `phase-b-lab-mirror-cookie-session.json` and `phase-b-lab-interaction-measured.json`. Check action response statuses and persisted GET projections independently. Two-coupon checkpoint: coffee total/tax 662/36; coupon amounts 95/5 and 190/10; cart discount/tax 284/16. Do not replace captured totals with sums of rounded coupon amounts. Removing LAB20 restores the one-coupon projection. This establishes lab behavior only.
5. **Explicit refusal without mutation.** Scope and constitution require invalid quantities (including zero), malformed/unsupported actions and unsupported identities to return an explicit refusal with a code and preserve the cart. Probe empty and populated states, missing/invalid fields, unknown references and variants. Uncaptured error text is not prescribed. Generic properties do not establish merchant outcomes for those probes.
6. **Explicit reset and seed isolation.** `reset(seed)` discards prior state, with no carryover across merchant/mirror/two-coupon seeds. Replaying a captured sequence after dirty state and reset must yield the same captured decisions/projections. LAB20 must not remain available after resetting to the merchant seed. Independent engine instances must not share cart state.
7. **Explicit copied snapshot and determinism.** Mutating returned line/coupon arrays, their objects or totals must not change engine state. Previously returned snapshots remain unchanged after dispatch/reset. Repeated snapshots and reset/replay must be deterministic. Every observed monetary output is a safe integer (scope/constitution).
8. **CLI contract.** Run all four discovery scenarios through the approved CLI; exercise its two existing modes after identifying usage, and run full `pnpm check`. Existing suite execution is permitted but is not an independent assertion source.

## Limits fixed before execution

No oracle for merchant coffee pricing at quantity 2/3, unmeasured coupon sequences, other products/variants, or native merchant tax-entry settings. Inclusive source pricing and restrictions are candidate assumptions. No second coupon combination mode, held-out evaluation, new capture, Woo startup, or dependency/configuration change. Prepared source price precision remains six decimals; no synthetic monetary oracle will be invented for other prices. Snapshot/reset/refusal properties are contract tests, not new merchant captures.

Role writes are limited to `tests/verify*` and this new trace directory. Work effort will be recorded here for Antoine to transfer into `docs/notes/cost-register.md`, whose modification is outside verify rights.
