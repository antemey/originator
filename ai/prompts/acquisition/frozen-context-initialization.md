# Operator authorization — frozen context setup, 2026-09-20

Executed operator instruction, preserved verbatim. This follows the geographic precondition stop; frozen recipe commitments are unchanged.

> on avance
> Authorize the frozen target-context initialization before each held-out recipe.
>
> After the validated Woo reset and empty-cart check, establish exactly the
> frozen guest context: FR / Paris / 75001, using the same Woo procedure
> already validated during the contemporary control.
>
> This is environment setup, not a preregistered recipe action; record it
> separately from the recipe trace.
>
> After context initialization, run one final cart preflight. Continue only if:
> - HTTP 200 and valid cart JSON;
> - guest context is FR / Paris / 75001;
> - zero lines and zero coupons.
>
> On 403/challenge, wrong context or non-empty cart, stop before any held-out
> action and preserve the technical evidence.
>
> Apply this same setup before each of the three recipes.
> All previously established acquisition, sanitation and stop rules remain
> unchanged.
>
> Proceed with acquisition only; stop before import or official evaluation.
