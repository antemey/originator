# Originator — working instructions

These instructions continue an existing setup; they are not a new scaffolding task. They replace V4 and the subsequent amendments as the operator's execution brief. Do not reread historical briefs to assemble competing requirements.

Keep `work-briefs/` beside `repo/`, not inside it. This coordination pack is outside the final repository. The delivered repository must still contain the active constitution, role briefs, the actual actionable prompts used, useful traces, and the knowledge base. Archive the actionable instructions actually read, not just a launch prompt with external paths or a retrospectively improved account. No runtime or reader command may depend on this external folder.

## Read only what the current phase needs

| Phase | Read in order | Stop at |
|---|---|---|
| A — align the completed setup | `01_SHARED_RULES.md`, `02_ALIGN.md` | Alignment report; no business preparation |
| B — prepare data and references | `01_SHARED_RULES.md`, `03_PREPARE.md` | Pre-implementation evidence checkpoint |
| C — implement and verify | Active repository instructions and selected role brief | Each role's handoff; at most three cycles |
| D — frozen evaluation and delivery | `01_SHARED_RULES.md`, `04_IMPLEMENT_AND_DELIVER.md` | Audited delivery commit; no push |

`04_IMPLEMENT_AND_DELIVER.md` defines the phase-C operator protocol as well. Phase A does not load the preparation or delivery brief; its own instructions contain the required alignment changes.

Shared rules prevail over stage wording. Before the evidence checkpoint, resolve any incompatibility with the existing contract explicitly. Afterwards, roles cannot amend frozen inputs: report the conflict to Antoine.

## Paths and phase-A launch

The current Codex session is open in the project parent containing `repo/`, `inputs/`, `held-out/` and `work-briefs/`. Read the pack from `work-briefs/`. Filenames in the reading table are relative to this folder. Repository paths elsewhere in the pack, including `../held-out/recipes/`, are resolved from `repo/`; run Git, pnpm and repository scripts there. Do not relocate or reinitialize the repository.

```text
You are already in the project parent containing repo/, inputs/, held-out/
and work-briefs/.

Read work-briefs/00_START.md, then work-briefs/01_SHARED_RULES.md and
work-briefs/02_ALIGN.md, in that order. Apply phase A only.
Read repo/AGENTS.md and the active repository instructions needed for
alignment. Synchronize them with this pack; do not reconstruct requirements
from historical briefs.

Run repository commands from repo/ and preserve existing work. Do not read
held-out/, start preparation, launch Woo, capture the merchant, implement
the engine, commit without authorization, or push.

V1 requires two percentage coupons in the lab with sequential discounts
disabled. No other combination mode is to be prepared or implemented.

Respond in French. Write new repository artifacts in English; preserve
original evidence and merchant labels. Stop with the alignment report.
```

For phase B, replace the phase and mission explicitly. Never chain phases or open an implementation session automatically.
